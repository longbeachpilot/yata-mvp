'use client'
import{useEffect,useMemo,useState}from'react';import Link from'next/link';import{useRouter}from'next/navigation';import{BookOpen,RotateCcw}from'lucide-react';import{supabase}from'@/lib/supabase'
type Skill={skill_key:string;score:number;note:string|null};type Logbook={id:string;booking_id:string;learner_id:string;instructor_id:string;minutes:number;instructor_note:string;next_goal:string|null;created_at:string;instructor:{id:string;name:string;vehicle:string}|null;booking:{lesson_type:string;lesson_date:string;start_time:string;pickup_text:string}|null;skills:Skill[]};const skillLabel:Record<string,string>={basic_control:'기본 조작',lane_keeping:'차선 유지',lane_change:'차선 변경',parking:'주차',highway:'고속도로',night_driving:'야간주행'}
export default function LogbookPage(){const router=useRouter();const[logs,setLogs]=useState<Logbook[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{async function load(){const{data:{user}}=await supabase.auth.getUser();if(!user){router.replace('/login?next=%2Flogbook');return}const{data:p}=await supabase.from('profiles').select('role').eq('id',user.id).maybeSingle();if(p?.role==='instructor'){router.replace('/dashboard/instructor');return}const{data,error}=await supabase.from('lesson_logs').select('id,booking_id,learner_id,instructor_id,minutes,instructor_note,next_goal,created_at,instructor:instructors(id,name,vehicle),booking:bookings(lesson_type,lesson_date,start_time,pickup_text),skills:skill_progress(skill_key,score,note)').eq('learner_id',user.id).order('created_at',{ascending:false});if(error)setError(error.message);else setLogs((data??[])as unknown as Logbook[]);setLoading(false)}load()},[router]);const totalMinutes=useMemo(()=>logs.reduce((s,l)=>s+Number(l.minutes||0),0),[logs]);function avg(k:string){const v=logs.flatMap(l=>l.skills||[]).filter(s=>s.skill_key===k).map(s=>+s.score);return v.length?Math.round(v.reduce((a,b)=>a+b,0)/v.length):null}if(loading)return <main className="container section"><div className="panel">Logbook을 불러오는 중...</div></main>;if(error)return <main className="container section"><div className="bookingError">Logbook을 불러오지 못했습니다: {error}</div></main>;return <main className="container section"><div className="pageHead"><div><span>YA TA LOGBOOK</span><h1>내 연수 기록</h1><p>완료한 수업과 다음 연습 목표를 이어서 관리합니다.</p></div><Link href="/map" className="primaryBtn">다음 수업 찾기</Link></div><div className="dashStats"><div><span>누적 연수</span><b>{(totalMinutes/60).toFixed(totalMinutes%60?1:0)}h</b><small>Logbook 기준</small></div><div><span>완료 기록</span><b>{logs.length}</b><small>작성된 수업</small></div><div><span>주차</span><b>{avg('parking')===null?'-':`${avg('parking')}%`}</b><small>기록 평균</small></div><div><span>차선 변경</span><b>{avg('lane_change')===null?'-':`${avg('lane_change')}%`}</b><small>기록 평균</small></div></div>{logs.length===0?<section className="panel" style={{marginTop:24,textAlign:'center',padding:40}}><BookOpen size={34}/><h3>아직 연수 기록이 없습니다.</h3><p>수업 완료 후 교관이 Logbook을 작성하면 이곳에 기록됩니다.</p><Link href="/map" className="primaryBtn">첫 수업 찾기</Link></section>:<div style={{display:'grid',gap:20,marginTop:24}}>{logs.map(log=>{const again=`/book?instructor=${log.instructor_id}${log.booking?.pickup_text?`&pickup=${encodeURIComponent(log.booking.pickup_text)}`:''}${log.booking?.lesson_type?`&purpose=${encodeURIComponent(log.booking.lesson_type)}`:''}`;return <section className="panel" key={log.id}><div style={{display:'flex',justifyContent:'space-between',gap:20,flexWrap:'wrap'}}><div><span>{log.booking?.lesson_date}</span><h2>{log.booking?.lesson_type||'운전 연수'}</h2><p>{log.instructor?.name||'교관'} 교관 · {log.minutes/60}시간 · {log.instructor?.vehicle}</p><p>{log.booking?.start_time} · {log.booking?.pickup_text}</p></div><span className="status">수업 완료</span></div>{(log.skills||[]).length>0&&<div style={{marginTop:20}}><h3>이번 수업 숙련도</h3><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(170px,1fr))',gap:12}}>{log.skills.map(s=><div key={s.skill_key} style={{padding:14,background:'#f7f7f7',borderRadius:12}}><div style={{display:'flex',justifyContent:'space-between'}}><strong>{skillLabel[s.skill_key]||s.skill_key}</strong><b>{s.score}%</b></div><div className="bar" style={{marginTop:8}}><i style={{width:`${s.score}%`}}/></div>{s.note&&<small>{s.note}</small>}</div>)}</div></div>}<div style={{marginTop:20,paddingTop:18,borderTop:'1px solid #eee'}}><h3>교관 코멘트</h3><p>{log.instructor_note}</p>{log.next_goal&&<div style={{padding:16,borderRadius:12,background:'#fff5ef'}}><strong>다음 수업 목표</strong><p>{log.next_goal}</p></div>}<Link href={again} className="primaryBtn" style={{marginTop:14}}><RotateCcw size={16}/> 같은 교관과 이어서 연습</Link></div></section>})}</div>}</main>}
