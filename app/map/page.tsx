import Link from 'next/link'; import { instructors } from '@/lib/data';
export default function MapPage(){return <main className="mapPage"><aside className="mapSidebar"><div className="mapTitle"><span>내 주변 교관</span><h1>강남구 · 12명</h1></div><div className="chipRow"><button>오늘 가능</button><button>장롱면허</button><button>교육차량</button><button>가격</button></div><div className="mapResults">{instructors.map((i,n)=><Link href={`/instructors/${i.id}`} className="miniInstructor" key={i.id}><div className="mapAvatar">{n+1}</div><div><strong>{i.name} 교관 <small>★ {i.rating}</small></strong><span>{i.specialties.join(' · ')}</span><span>{i.area}</span><b>{i.price.toLocaleString()}원 / 2시간</b></div></Link>)}</div></aside><section className="mapCanvas"><div className="mapGrid"/><div className="mapRoad r1"/><div className="mapRoad r2"/><div className="mapRoad r3"/><div className="youPin">현재 위치</div>{instructors.map((i,n)=><Link key={i.id} href={`/instructors/${i.id}`} className={`mapPin p${n+1}`}>{n+1}<span>{i.name}</span></Link>)}</section></main>}
